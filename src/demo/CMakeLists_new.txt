add_library(demo)

target_sources(${CMAKE_CURRENT_LIST_DIR}/demo.cpp
)

target_include_directories(${CMAKE_CURRENT_LIST_DIR})

# Pull in pico libraries that we need
target_link_libraries(demo pico_stdlib)
